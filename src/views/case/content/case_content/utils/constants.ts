// 树形组件配置
export const defaultProps = {
  children: 'children',
  label: 'label'
}

// 动画变体配置
export const actionBtnVariants = {
  hover: { scale: 1.1, backgroundColor: '#f0f0f0' },
  tap: { scale: 0.9 }
}